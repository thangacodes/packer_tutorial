```bash
How to build a custom image in AWS?

1) Clone the repository
   git clone https://github.com/thangacodes/packer_tutorial.git
2) Navigate to the folder where the project Packer configs are located:
   cd Packer/project-8/
3) I’ve included an executable Bash script to help us to run packer commands
   ls -lrt
   chmod a+x image_builder.sh
   ./image_builder.sh
